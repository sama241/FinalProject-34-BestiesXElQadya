apiVersion: v1
kind: Service
metadata:
  name: apigateway-service
  labels:
    app: apigateway-service
spec:
  type: NodePort
  selector:
    app: apigateway-service
  ports:
    - port: 80
      targetPort: 8761
      nodePort: 30001
